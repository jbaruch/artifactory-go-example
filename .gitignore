# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

#output of goreleaser
dist

#jfrogcli executabe
jfrog

#exclude binary, but not source
hello
!hello.go